# Rename to keystore.properties (gitignored) to enable the playRelease build type.
# Generate your Play Store upload key in Google Play Console → Setup → App signing.
storeFile=path/to/your-play-upload-key.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
